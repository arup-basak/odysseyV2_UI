import React from "react";

const BackgroundImage = () => {
  return (
    <div className="w-full h-full absolute overflow-hidden inset-0 bg-[#31343E]">
      {/* <div style={{
        backgroundImage: "linear-gradient(to right top, #e77b5f, #e96672, #df578b, #c853a6, #a058bf, #836ed6, #5a81e6, #0091ef, #00aff7, #00caf2, #14e1e5, #7ef5d8)",
        width: '100%',
        height: '100%'
      }}>
      </div>
      <div style={{
        position: 'absolute',
        top: 0,
        left: 0,
        right: 0,
        bottom: 0,
        backdropFilter: 'blur(20px) saturate(180%)', // Increased blur and added saturation
        backgroundColor: 'rgba(255, 255, 255, 0.3)' // Slightly more opaque white tint
      }}>
      </div> */}
    </div>
  );
};

export default BackgroundImage;